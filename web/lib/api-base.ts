// Forager is file-first: the web client reads JSON from the deployed site.
// Keep the old exports so the upstream UI components remain unchanged.
export const API_BASE = "";
export const USE_API = false;
